/**
 * Image Service — application-layer facade.
 * Wraps the sketch-pilot image service factory for use in the backend.
 */
export type { ImageProvider, ImageService, ImageServiceConfig } from '@sketch-pilot/services/image'
export { ImageServiceFactory } from '@sketch-pilot/services/image'
